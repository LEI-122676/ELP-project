
const a := valor_a.         ### 'valor_a' é 5
mut b := 6 == 2.

if (a < b) << print(1). >>
else <<
    const text := "O 'a' é maior" + " ou igual ao 'b'".
    print(text).
>>


const iteravel := valores_array.

for (element >>> iteravel) <<               ### Não há whiles
    const extracted := iteravel..element.

    if (extracted > 5) <<
        break.
    >>

    print("Valor extraído: " + extracted).
>>